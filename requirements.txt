# 기본 라이브러리
python-dotenv~=1.0.0
pyyaml~=6.0.1
requests~=2.31.0
aiohttp~=3.9.1
asyncio~=3.4.3
tqdm~=4.66.1
pandas~=2.1.3
numpy~=1.26.2
pathlib~=1.0.1
typing-extensions~=4.8.0
concurrent-futures~=3.0.5

# AI/ML 관련
openai~=1.3.7
sentence-transformers~=2.2.2
chromadb~=0.4.18
torch~=2.1.1  # sentence-transformers 의존성
transformers~=4.35.2  # sentence-transformers 의존성

# 검색 및 데이터 수집
serpapi~=0.1.0
tavily-python~=0.2.8
beautifulsoup4~=4.12.2
lxml~=4.9.3
google-search-results~=2.4.2  # SerpAPI 의존성

# PDF 생성 관련
weasyprint~=60.1
markdown~=3.5.1
cssselect2~=0.7.0  # WeasyPrint 의존성
cffi~=1.16.0  # WeasyPrint 의존성
pyphen~=0.14.0  # WeasyPrint 의존성
tinycss2~=1.2.1  # WeasyPrint 의존성

# 벡터 데이터베이스 의존성
hnswlib~=0.7.0  # ChromaDB 의존성
pydantic~=2.5.2  # ChromaDB 의존성

# 유틸리티
python-dateutil~=2.8.2
pytz~=2023.3
colorama~=0.4.6  # Windows 콘솔 색상 지원
rich~=13.7.0  # 콘솔 출력 개선
dataclasses~=0.6  # Python 3.7 미만에서 필요

# 테스트 및 개발 도구
pytest~=7.4.3
black~=23.11.0  # 코드 포맷팅
flake8~=6.1.0  # 코드 린팅
mypy~=1.7.1  # 타입 체크

# 운영체제별 필수 의존성
# macOS:
#   brew install pango cairo gdk-pixbuf
# Linux:
#   apt-get install libpango1.0-dev libcairo2-dev libgdk-pixbuf2.0-dev
# Windows:
#   WeasyPrint 설치 프로그램 (https://weasyprint.org/download/)

# 설치 순서:
# 1. 운영체제별 필수 라이브러리 설치
# 2. pip install -r requirements.txt
